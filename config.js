window.APP_CONFIG = {
  // Keep empty in demo/frontend-only mode.
  // For production, route through a secure backend instead of exposing credentials in the browser.
  EBAY_APP_ID: "",
  ENABLE_LIVE_EBAY_COMPS: false,
  ENABLE_DEMO_EBAY_COMPS: true,
};
